@model WebApplication1.ViewModels.CourseClassFormViewModel
@{
    ViewData["Title"] = "Edit Course Class";
    Layout = "~/Views/Shared/_DashboardLayout.cshtml";
}

<h4>Edit Course Class</h4>
<form asp-action="Edit" method="post">
    <input type="hidden" asp-for="Id" />
    <div class="mb-3">
        <label asp-for="ClassCode" class="form-label"></label>
        <input asp-for="ClassCode" class="form-control" />
        <span asp-validation-for="ClassCode" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="SubjectId" class="form-label"></label>
        <select asp-for="SubjectId" class="form-select">
            <option value="">-- Select Subject --</option>
            @{
                var subjects = (ViewBag.Subjects as List<WebApplication1.ViewModels.SubjectListViewModel>) ?? new List<WebApplication1.ViewModels.SubjectListViewModel>();
            }
            @foreach (var s in subjects)
            {
                <option value="@s.Id" selected="@(Model.SubjectId == s.Id ? "selected" : null)">@s.SubjectCode - @s.SubjectName</option>
            }
        </select>
    </div>

    <div class="mb-3">
        <label asp-for="LecturerId" class="form-label"></label>
        <select asp-for="LecturerId" class="form-select">
            <option value="">-- Select Lecturer --</option>
            @{
                var lecturers = (ViewBag.Lecturers as List<WebApplication1.ViewModels.LecturerListViewModel>) ?? new List<WebApplication1.ViewModels.LecturerListViewModel>();
            }
            @foreach (var l in lecturers)
            {
                <option value="@l.Id" selected="@(Model.LecturerId == l.Id ? "selected" : null)">@l.LecturerCode - @l.FullName</option>
            }
        </select>
    </div>

    <div class="mb-3">
        <label asp-for="Semester" class="form-label"></label>
        <input asp-for="Semester" class="form-control" />
    </div>

    <div class="mb-3">
        <label asp-for="MaxStudents" class="form-label"></label>
        <input asp-for="MaxStudents" class="form-control" type="number" />
    </div>

    <div class="mb-3">
        <label asp-for="Room" class="form-label"></label>
        <input asp-for="Room" class="form-control" />
    </div>

    <div class="mb-3">
        <label asp-for="Status" class="form-label"></label>
        <select asp-for="Status" class="form-select" asp-items="Html.GetEnumSelectList<WebApplication1.Models.CourseClassStatus>()"></select>
    </div>

    <button type="submit" class="btn btn-primary">Save</button>
</form>